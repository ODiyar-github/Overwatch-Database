/**
 * Die Klasse Programm enthält die main-Methode und startet die Anwendung.
 */
public class Programm {
	
	/**
	 * Die main-Methode, die beim Start der Anwendung aufgerufen wird.
	 * Erstellt eine Instanz der Navigation-Klasse, um die Anwendung zu starten.
	 * @param args Die Argumente, die beim Start der Anwendung übergeben werden können (keine Verwendung).
	 */
	public static void main(String[] args) 
	{
		new Navigation();
	}
	
}